<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Habilita "crear" solo para un grupo específico -->
    <record id="view_pos_order_tree_with_create" model="ir.ui.view">
        <field name="name">pos.order.tree.with.create</field>
        <field name="model">pos.order</field>
        <field name="inherit_id" ref="point_of_sale.view_pos_order_tree"/>
        <field name="priority" eval="20"/>
        <!-- <field name="groups_id" eval="[(4, ref('pos_import_enable.group_import_pos_order'))]"/> -->
        <field name="arch" type="xml">
            <tree position="attributes">
                <attribute name="create">true</attribute>
            </tree>
        </field>
    </record>
</odoo>

